{
  "name": "staticcheck",
  "version": "latest",
  "build": {
    "steps": [
      "mkdir -p build && go build -o build/staticcheck honnef.co/go/tools/cmd/staticcheck"
    ],
    "bin": "staticcheck"
  }
}
